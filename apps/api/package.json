{
  "name": "@tron-payments/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "db:migrate": "echo 'TODO: drizzle-kit migrate'",
    "db:studio": "echo 'TODO: drizzle-kit studio'",
    "validate:config": "tsx src/scripts/validate-config.ts"
  },
  "dependencies": {
    "@tron-payments/shared": "*",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
